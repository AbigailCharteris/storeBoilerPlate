import React from 'react';
import { shallow } from 'enzyme';
import { assert, expect } from 'chai';
import DeltaListComponent from '../../../routes/batch/deltaListComponent';

describe('DeltaListComponent Component', () => {
  const props = {
    dataList: [
      {
        id: '69d54e9e-5216-4881-b3a6-40020eea66cc',
        batchName: 'Monday',
        version: '20170511114104',
        deltaComment: 'test comment',
        scenarioIdList: ['7098bd2e-550a-4920-aad0-dc0b48ced2c0', 'archived'],
        auditList: [
          {
            updatedBy: 'unused',
            timestamp: '20170511114104',
          },
        ],
      },
    ],
    added: [],
    removed: [],
    header: 'Test',
    subHeader: '',
    id: '123456',
    value: 'test',
  };

  beforeEach(() => {});

  afterEach(() => {});

  it('Should render the list', () => {
    const wrapper = shallow(<DeltaListComponent {...props} />);

    const element = wrapper.find('.delta-list-group').text();
    expect(element).to.be.eql('<ul />');
  });
});
