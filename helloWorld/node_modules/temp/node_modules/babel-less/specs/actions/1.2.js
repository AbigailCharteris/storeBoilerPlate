import { assert, expect } from 'chai';
import action from '../../actions/requestBatchesAction';
import { REQUEST_BATCHES } from '../../constants/actionTypes';

describe('requestBatchesAction', () => {
  it('should generate action', () => {
    const actualAction = action();

    expect(actualAction).to.deep.equal({
      type: REQUEST_BATCHES,
    });
  });
});
