import { assert, expect } from 'chai';
import action from '../../actions/requestScenariosAction';
import { REQUEST_SCENARIOS } from '../../constants/actionTypes';

describe('requestScenariosAction', () => {
  it('should generate action', () => {
    const actualAction = action();

    expect(actualAction).to.deep.equal({
      type: REQUEST_SCENARIOS,
    });
  });
});
