class AuditEntry {
    constructor(updatedBy, timestamp, batch, scenario, action) {
      this.updatedBy = updatedBy;
      this.timestamp = timestamp;
      this.batch = batch;
      this.scenario = scenario;
      this.action = action;
    }
  }
  
  export default AuditEntry;
  