import { SCENARIO_VERSION_LIST } from '../constants/stateConstants';

export const fromModel = action => ({
  [SCENARIO_VERSION_LIST]: action.payload[SCENARIO_VERSION_LIST] || {},
});

export const toModel = scenarioVersionDetail => ({
  payload: {
    [SCENARIO_VERSION_LIST]: scenarioVersionDetail,
  },
});
