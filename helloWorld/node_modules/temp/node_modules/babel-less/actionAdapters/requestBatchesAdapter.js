import { BATCH_LIST, BATCH_LIST_NO_DATA } from '../constants/stateConstants';

export const fromModel = action => ({
  [BATCH_LIST]: action.payload[BATCH_LIST] || BATCH_LIST_NO_DATA,
});

export const toModel = (batchList = []) => ({
  payload: {
    [BATCH_LIST]: batchList,
  },
});
