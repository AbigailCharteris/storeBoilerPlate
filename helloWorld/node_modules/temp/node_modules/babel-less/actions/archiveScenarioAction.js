import { ARCHIVE_SCENARIO } from '../constants/actionTypes';
import toModel from '../actionAdapters/archiveScenariosAdapter';

const archiveScenario = scenariosList => ({
  type: ARCHIVE_SCENARIO,
  ...toModel(scenariosList),
});

export default archiveScenario;
