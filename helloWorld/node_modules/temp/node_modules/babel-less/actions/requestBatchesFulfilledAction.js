import { REQUEST_BATCHES_FULFILLED } from '../constants/actionTypes';
import { toModel } from '../actionAdapters/requestBatchesAdapter';

const requestScenariosFulfilled = batches => ({
  type: REQUEST_BATCHES_FULFILLED,
  ...toModel(batches),
});

export default requestScenariosFulfilled;
