import { REQUEST_SCENARIOS_AUDIT_FULFILLED } from '../constants/actionTypes';
import { toModel } from '../actionAdapters/requestScenariosFulfilledAdapter';

const requestScenariosAuditFulfilled = scenarioList => ({
  type: REQUEST_SCENARIOS_AUDIT_FULFILLED,
  ...toModel(scenarioList),
});

export default requestScenariosAuditFulfilled;
